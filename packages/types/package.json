{
  "name": "@buzz/types",
  "type": "commonjs",
  "version": "1.0.0",
  "description": "A shared TypeScript types package used across both the web and server applications in our monorepo. This package provides type definitions for consistent type safety and reusability throughout the project.",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm --dts",
    "dev": "tsup src/index.ts --format cjs,esm --dts --watch"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "tsup": "^8.2.4",
    "typescript": "^5.5.4"
  }
}
